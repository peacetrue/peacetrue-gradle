println("${project.name} build.gradle start")

tasks.register('println') {
    println("${project.name}.test init")
    doLast {
        println("${project.name}.test execute")
    }
}

println("${project.name} build.gradle end")
